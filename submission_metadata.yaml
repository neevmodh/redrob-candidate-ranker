# Redrob Hackathon — Submission Metadata
# Team: LeConHinton

# ============================================================================
# Team identity
# ============================================================================
team_name: "LeConHinton"

primary_contact:
  name: "Neev Modh"
  email: "neevmodh205@gmail.com"
  phone: "+91-7016111267"   # fill in your phone number

team_members:
  - name: "Neev Modh"
    email: "neevmodh@gmail.com"
    role: "ML Engineer / Team Lead"

# ============================================================================
# Code and reproducibility
# ============================================================================
github_repo: "https://github.com/neevmodh/redrob-candidate-ranker"

sandbox_link: "https://redrobai-candidate-ranker.streamlit.app/"

reproduce_command: "python rank.py --candidates ./candidates.jsonl --out ./leconhinton.csv"

# ============================================================================
# Compute environment
# ============================================================================
compute:
  platform: "MacBook AIR 4 (Apple Silicon M-series)"
  cpu_cores: 8
  ram_gb: 16
  python_version: "3.14.3"
  os: "macOS (Darwin arm64)"
  uses_gpu_for_inference: false
  has_network_during_ranking: false
  pre_computation_required: false
  pre_computation_time_minutes: 0

# ============================================================================
# AI tools declaration
# ============================================================================
ai_tools_used:
  - "Kiro (Claude)"

ai_usage_summary: |
  Used Kiro (Claude-based) for architecture discussion, planning, and code review.
  All scoring logic (skill trust scoring, career trajectory analysis, services-company
  penalty, behavioral multiplier) is hand-engineered based on careful JD analysis.
  No candidate data was fed to any LLM. The final system is fully deterministic
  feature engineering with zero external API calls.

# ============================================================================
# Approach summary
# ============================================================================
methodology_summary: |
  Multi-signal feature-engineering ranker with anti-keyword-stuffing design.
  Five scoring components: skill match (35%), career trajectory (30%), experience
  (20%), education (5%), and a behavioral availability multiplier (applied post-score).

  Skill scoring uses a trust multiplier (endorsements x duration x proficiency) so
  "expert FAISS with 0 months used" scores near zero, catching keyword stuffers.
  Career trajectory applies a services-company penalty (TCS/Infosys/Wipro/Accenture
  careers score 0.25x multiplier) and requires JD-relevant keywords in role
  descriptions, not just in the skills list. Behavioral signals (open_to_work,
  last_active_date, recruiter_response_rate, notice_period) are combined into a
  multiplier in [0.50, 1.20] applied to the base score.

  Honeypot detection flags candidates with impossible-profile signals: expert skills
  with 0 months used, impossible tenure at young companies (Sarvam AI/Krutrim),
  inflated career tenure ratios. 72 detected, 0 in top-100. Runtime: ~25 seconds
  on CPU for 100K candidates. No GPU, no network, no model downloads.

# ============================================================================
# Declarations
# ============================================================================
declarations:
  read_submission_spec: true
  code_is_original_work: true
  no_collusion: true
  honeypot_check_done: true   # 72 honeypots detected, 0 in top-100
  reproduction_tested: true
